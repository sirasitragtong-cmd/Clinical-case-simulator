# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Node / Environment
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs & Temporary files
*.log
*.tmp
*.bak

# IDE & Editor settings
.vscode/
.idea/
*.swp
*.swo

# Test / Scratch scripts (optional local dev files)
integration-test.cjs

# Local dev helpers (not part of the production bundle)
.claude/

# NOTE: css/style.min.css IS committed on purpose. GitHub Pages serves the
# repository as-is and never runs `node build.js`, so the compiled stylesheet
# has to be in the tree or the deployed site loads with no CSS at all.

# Dev-only avatar contact sheet (node tools/preview-avatar.js)
avatar-preview.png
_*.html
